# レガシー解析・設計書自動生成エージェント
#プロジェクト名
- legacyDoc
## ハイブリッド型マルチエージェント Webアプリ 仕様書

* 文書版数: 0.1
* 作成日: 2026-04-09
* 形式: Markdown
* 想定利用者: 個人開発者 / 小規模チーム
* 目的: レガシーシステムのソースコード解析から、保守運用に必要な各種設計書を自動生成する Web アプリケーションの実装仕様を定義する

\---

## 1\. 背景と目的

既存のレガシープログラムについて、設計書の紛失、開発担当者の退職、機能把握困難により、保守運用の継続が危機的な状態にある。  
本システムは、複数ソースファイルから成る既存プロジェクトを解析し、構造・処理・画面・DB更新の観点で設計情報を抽出し、Markdown および PlantUML ベースの図表として整理・生成することを目的とする。

本システムは、Claude Code のような対話型利用感を持ちつつ、内部的には **ワークフロー型 + ReAct 型のハイブリッド構成を採用したマルチエージェント** とする。

\---

## 2\. システムの到達目標

本システムは、アップロードされたソフトウェアプロジェクトに対して以下を実現する。

1. ソースコード、設定ファイル、SQL 定義、テンプレート、画面定義などを横断解析する
2. 言語やフレームワークを推定する
3. クラス・関数・画面・DB 更新箇所・処理フローなどを抽出する
4. 不完全情報がある場合でも、推定根拠付きで設計情報を構造化する
5. ユーザがチャットで補足指示を出しながら、再解析や特定範囲の深掘りができる
6. プロジェクトごとに Input / 中間成果物 / Output を分離管理する
7. 生成された設計書や図を Web UI 上で閲覧できる

\---

## 3\. 対象範囲

### 3.1 対象言語

* C
* C++
* Java

### 3.2 対象となる関連情報

ソースコード本体に加え、以下も解析対象とする。

* ヘッダファイル
* ビルド設定
* 設定ファイル
* SQL ファイル
* XML
* アノテーション
* テンプレート
* 画面定義ファイル
* ログ出力定義
* DB マッピング定義
* フレームワーク設定

### 3.3 対象とする追加判断

ユーザから明示されなくても、以下を解析から推定対象とする。

* 利用言語
* 利用フレームワーク
* 画面系技術
* DB アクセス方式
* ORM / マッパー利用有無
* MyBatis XML の SQL
* JPA アノテーションベースのエンティティ / SQL 関連
* アーキテクチャパターン
* バッチ系 / Web系 / GUI系 / CLI系の別

\---

## 4\. 生成対象ドキュメント

出力は Markdown と PlantUML を基本とし、図は SVG に変換して保存する。

### 4.1 生成対象一覧

* 全体処理概要
* クラス一覧
* クラス図
* クラス仕様
* メソッド一覧
* メソッド処理概要
* フローチャート
* シーケンス図
* 画面遷移図
* 画面項目仕様書
* 画面イベント一覧
* 画面レイアウト
* CRUD表
* DB更新仕様

### 4.2 出力形式

* 文書: `.md`
* 図定義: `.puml`
* 図画像: `.svg`
* 中間構造データ: `.json`

\---

## 5\. 基本方針

### 5.1 アーキテクチャ方針

本システムは以下の思想で設計する。

* 外側は **ワークフロー型**
* 各解析フェーズ内部は **ReAct 型**
* 役割別に **マルチエージェント** を分離
* 解析結果はすべて中間構造に正規化してから文書生成する

### 5.2 採用理由

ワークフロー型だけでは柔軟な探索が弱く、ReAct 型だけでは解析の暴走や粒度不整合が起こりやすい。  
そのため、フェーズ制御・状態管理・成果物管理はワークフローで行い、個別の探索・再読込・再推論は ReAct 的に実行する構成とする。

### 5.3 3層解析アーキテクチャ

コード解析は以下の3層で段階的に実施する。各層は後の層の入力となり、精度と抽象度が段階的に上がる構成とする。

```text
Layer 1: 構文解析層（Syntax Layer）
  ├─ Tree-sitter によるAST構築
  ├─ シンボル（クラス、関数、変数）の抽出
  └─ 単一ファイル内の構造情報
      ↓
Layer 2: シンボル解決層（Symbol Resolution Layer）
  ├─ import / include の追跡
  ├─ ファイル横断の型解決・名前解決
  ├─ 呼び出し先クラス・メソッドの特定
  └─ 継承・実装関係の完全解決
      ↓
Layer 3: 意味推論層（Semantic Inference Layer）
  ├─ LLM による責務推定
  ├─ 処理の意図・ビジネスロジック推定
  ├─ モジュール境界・レイヤ判定
  └─ 不明点の推定根拠付き補完
```

#### Layer 1: 構文解析層

* Tree-sitter を用いて各ファイルの AST を構築する
* クラス名、メソッド名、引数、戻り値、フィールド宣言を抽出する
* 単一ファイル内で完結する情報のみを扱う
* 処理コスト: 低（秒単位）

#### Layer 2: シンボル解決層

* Layer 1 の結果を入力とし、ファイル横断の情報を構築する
* Java: package + import 文から完全修飾名を構築し、型を解決する
* C/C++: `#include` チェーンを辿り、ヘッダファイルの定義を集約する
* 呼び出し関係: `obj.method()` の `obj` の型を解決し、呼出先クラスを特定する
* 継承・実装: インターフェース経由の呼出しは実装クラスの候補を列挙する
* 処理コスト: 中（プロジェクト規模に依存）
* MVP では基本的なシンボル解決（import/include 追跡、フィールド型解決）を実装し、高度な解決（ポリモーフィズム、ジェネリクス等）は将来拡張とする

#### Layer 3: 意味推論層

* Layer 1 + Layer 2 の結果をコンテキストとして LLM に渡す
* クラスの責務、処理の目的、ビジネスロジックを推定する
* 全出力に推定根拠（Evidence）と信頼度（confidence）を付与する
* 処理コスト: 高（LLM API 呼出し回数に依存）

### 5.4 解析精度方針

本システムの出力には「確定情報」と「推定情報」の2種類がある。

* **確定情報**: コードから機械的に抽出した事実（クラス名、メソッドシグネチャ、import 文、SQL 文等）
* **推定情報**: LLM またはヒューリスティクスにより推定した内容（責務、処理目的、レイヤ判定等）

出力ドキュメントでは、推定情報には必ず `[推定]` マーカーと confidence 値を付与し、確定情報と明確に区別する。

\---

## 6\. 想定ユースケース

### 6.1 プロジェクト解析

1. ユーザが Web UI でプロジェクト一式をアップロード
2. システムがプロジェクトを登録
3. 自動で前処理・構成解析・言語判定・フレームワーク推定を実施
4. エージェント群が段階的に設計情報を抽出
5. 中間成果物を蓄積
6. 最終的に設計書群を生成
7. ユーザが設計書を Web 上で閲覧

### 6.2 チャットによる再解析

1. ユーザが「このクラスの責務を詳しく」などの指示を送信
2. 関連成果物と元コードを再読込
3. 必要に応じて部分再解析
4. 設計書を更新または追加生成

### 6.3 複数プロジェクト管理

1. ユーザが複数の案件を個別登録
2. プロジェクトごとに Input / 中間成果物 / Output を保持
3. UI 上で案件を切り替えてチャットと成果物を参照

\---

## 7\. システム全体構成

### 7.1 全体構成要素

* Web フロントエンド
* API サーバ
* ジョブオーケストレータ
* マルチエージェント実行基盤
* ソース解析モジュール
* ドキュメント生成モジュール
* 図生成モジュール
* 成果物ストレージ
* プロジェクト管理ストア（JSON ファイルベース）

### 7.2 論理構成

```text
\[Web UI]
   ↓
\[API Server]
   ↓
\[Project / Chat / Job Controller]
   ↓
\[Workflow Orchestrator]
   ├─ Ingestion Agent
   ├─ Language / Framework Detection Agent
   ├─ Code Structure Analysis Agent
   ├─ Behavior Analysis Agent
   ├─ UI Analysis Agent
   ├─ DB Analysis Agent
   ├─ Document Synthesis Agent
   └─ Review / Gap Detection Agent
   ↓
\[Artifact Store]
```

\---

## 8\. マルチエージェント構成

### 8.1 エージェント一覧

#### 8.1.1 Ingestion Agent

役割:

* アップロードファイルの展開
* 文字コード判定
* ディレクトリ構造収集
* 除外対象判定
* プロジェクトメタ情報作成

入力:

* アップロードファイル一式

出力:

* ファイル一覧
* ディレクトリ構造
* 前処理済み入力情報

#### 8.1.2 Language / Framework Detection Agent

役割:

* 言語判定
* ビルドツール判定
* フレームワーク推定
* DB 利用方式推定
* UI 種別推定

出力:

* 技術スタック推定結果
* 根拠一覧

#### 8.1.3 Code Structure Analysis Agent

役割:

* クラス / 構造体 / 関数 / メソッドの抽出（Layer 1: 構文解析）
* ファイル横断のシンボル解決（Layer 2: シンボル解決）
  * import / include の追跡による型解決
  * フィールド型の解決による依存関係の特定
  * 呼び出し先クラス・メソッドの特定
* 継承 / 実装 / 呼び出し関係の抽出
* モジュール責務推定（Layer 3: LLM 活用）

出力:

* クラス一覧（シンボル解決済み）
* メソッド一覧（呼出先解決済み）
* シンボルテーブル（完全修飾名 → 定義位置のマッピング）
* クラス図用中間データ（解決済み依存関係含む）
* 呼び出しグラフ（解決精度付き: 確定 / 候補 / 不明）

#### 8.1.4 Behavior Analysis Agent

役割:

* 処理フロー解析
* メソッド単位の処理概要抽出
* 呼び出し系列からシーケンス抽出
* フローチャート生成用中間情報作成

出力:

* 全体処理概要
* メソッド処理概要
* シーケンス候補
* フローチャート候補

#### 8.1.5 UI Analysis Agent

役割:

* 画面定義、テンプレート、イベント、遷移を解析
* 画面項目、イベント、レイアウトを推定
* GUI / Web / 独自画面の構成を整理

出力:

* 画面遷移図情報
* 画面項目仕様
* 画面イベント一覧
* 画面レイアウト情報

#### 8.1.6 DB Analysis Agent

役割:

* SQL、ORM、Mapper、Repository を解析
* CRUD の対応関係を整理
* DB 更新トリガとなる処理を抽出

出力:

* CRUD表
* DB更新仕様
* テーブル操作一覧

#### 8.1.7 Document Synthesis Agent

役割:

* 中間成果物を Markdown 文書へ整形
* PlantUML を生成
* 生成した PlantUML の構文チェックを行い、構文エラーがある場合は LLM に再生成を指示する（最大リトライ回数を設定）
* 図と文章の相互整合を取る
* **確定情報と推定情報を区別してマーキングする**
* **生成物間の一貫性チェックを行う**
  * クラス一覧のIDセット == クラス図のノードセット
  * メソッド一覧のIDセット ⊇ CRUD表で参照されるメソッドセット
  * 不整合があれば警告として出力する

出力:

* 各種 `.md`（推定箇所にマーカー付き）
* 各種 `.puml`（構文検証済み）
* 一貫性チェック結果

#### 8.1.8 Review / Gap Detection Agent

役割:

* 設計情報の不足箇所検出
* 矛盾検出
* 推定の信頼度低い箇所の再解析依頼
* ユーザへの確認質問候補作成
* **エラーリカバリ判定**: 解析失敗ファイルの一覧と影響範囲を特定する

出力:

* 不足箇所一覧
* 再解析タスク
* 根拠不足警告
* 解析失敗レポート（失敗ファイル一覧、影響範囲、代替推定の有無）

\---

## 9\. ハイブリッド型の制御方式

### 9.1 ワークフロー型で制御する内容

* プロジェクト登録
* 解析ジョブ開始
* フェーズ遷移
* 再解析管理
* 途中成果物保存
* 最終出力生成
* UI への状態反映

### 9.2 ReAct 型で実行する内容

各エージェント内部で以下のループを許可する。

1. 状況理解
2. 次に読むべきファイル選定
3. 必要な追加解析
4. 解析結果の更新
5. 不足時の再探索

### 9.3 終了条件

各エージェントは以下で終了とする。

* 最大ステップ数到達
* 必要成果物が揃った
* 追加探索で新情報が増えない
* 上位オーケストレータが打ち切り判断した

### 9.4 LLM コンテキスト管理戦略

大規模プロジェクトでは全ファイルを LLM に渡すことは不可能なため、以下の段階的コンテキスト構築戦略を採用する。

#### 9.4.1 段階的サマリ構築

1. **ファイルインデックス作成**: 全ファイルの「1行サマリ」を作成する（ファイルパス + 主要シンボル + 推定責務）
2. **全体アーキテクチャ推定**: ファイルインデックスを LLM に渡し、全体のアーキテクチャ・レイヤ構造を推定する
3. **重要ファイル選定**: アーキテクチャに基づいて、詳細解析の優先度を決定する
4. **詳細解析**: 選定されたファイルの詳細解析を行う（関連ファイルのコンテキストを付加）
5. **統合**: 詳細解析結果 + 全体サマリから最終的な設計書を生成する

#### 9.4.2 コンテキスト予算管理

* エージェントごとに LLM 呼出し回数の上限を設定する
* 1回の LLM 呼出しに含めるソースコードは、トークン上限の 60% 以内とし、残りを指示・出力に充てる
* 長大なファイルはメソッド単位に分割して処理する
* 解析済みの中間結果を要約してコンテキストに含めることで、情報の損失を最小化する

#### 9.4.3 関連コード選択

特定のクラス/メソッドを LLM で解析する際、以下の関連情報をコンテキストに含める。

* 解析対象のソースコード全文
* 同一ファイル内の他メソッド（シグネチャのみ）
* import / include で参照しているクラスのシグネチャ
* 呼び出し元メソッドのシグネチャ（最大 5 件）
* 呼び出し先メソッドの実装（直接呼出しのみ、深さ 1）

### 9.5 エラーリカバリと部分成功

* 各ファイルの解析結果に `status` を付与する: `success` / `partial` / `failed`
* 一部のファイル解析が失敗しても、成功分で設計書を生成する
* 失敗ファイルは「解析レポート」としてユーザに通知する
* 失敗原因の分類: パースエラー / 文字コードエラー / トークン超過 / LLM エラー

\---

## 10\. 対応モデル方針

### 10.1 想定モデル

* Azure OpenAI
* Ollama 上の gpt-oss-120b

### 10.2 モデル抽象化

モデル差異を吸収するため、以下の共通インターフェースを実装する。

```ts
interface ModelAdapter {
  generate(input: GenerateInput): Promise<GenerateOutput>
  toolCall(input: ToolCallInput): Promise<ToolCallOutput>
  stream(input: StreamInput): AsyncIterable<string>
  supportsJsonSchema(): boolean
  supportsToolCalling(): boolean
}
```

### 10.3 使い分け方針

* 計画作成、統合判断、レビュー: Azure OpenAI 優先
* ローカル解析、部分再生成、大量読み込み: Ollama も選択可能
* 実装上は設定で切替可能にする

\---

## 11\. 機能要件

## 11.1 プロジェクト管理機能

* プロジェクトを新規作成できる
* プロジェクトに名前、説明、タグを設定できる
* プロジェクトごとにアップロード対象を分離管理できる
* プロジェクトごとに解析履歴を保持できる
* プロジェクトの Input / 中間成果物 / Output を分離保存できる

## 11.2 ファイルアップロード機能

* zip または複数ファイルアップロードに対応する
* ディレクトリ構造を保って登録できる
* 大容量プロジェクトでも段階的に処理できる
* 再アップロード時にバージョン管理できる

## 11.3 自動解析機能

* ソースコードを横断解析できる
* 言語混在プロジェクトを扱える
* フレームワークや DB 関連設定を自動判定できる
* エントリーポイント候補を抽出できる
* 呼び出し関係を抽出できる
* UI / DB / バッチ処理を区別して解析できる

## 11.4 チャット機能

* プロジェクト単位でチャットできる
* 解析結果を前提に追加質問できる
* 特定クラス、特定画面、特定 SQL の深掘りができる
* チャットにより再解析や再出力を起動できる
* 途中経過を一部表示できる

## 11.5 設計書生成機能

* 指定形式の Markdown を生成できる
* PlantUML を生成できる
* SVG に変換して表示できる
* 個別文書だけの再生成ができる
* 推定根拠を補足情報として保持できる

## 11.6 成果物閲覧機能

* 文書プレビューができる
* SVG 図を表示できる
* 生成履歴を閲覧できる
* 文書ごとの差分確認ができる
* 中間成果物の参照ができる

\---

## 12\. 非機能要件

### 12.1 前提

個人利用または小規模チーム利用を想定し、厳格なセキュリティ、監査、権限制御は本仕様では対象外とする。

### 12.2 性能

* 中規模プロジェクトで段階的解析が可能であること
* 長時間処理はジョブ形式で管理すること
* 再解析時は差分対象中心に処理すること

### 12.3 保守性

* エージェント追加が容易であること
* 言語対応を増やしやすい構造であること
* 文書テンプレート差し替えが容易であること

### 12.4 可観測性

* ジョブ状態を UI で確認できること
* 解析ログを段階的に参照できること
* エラー箇所が識別できること

\---

## 13\. データ管理方針

### 13.1 プロジェクト単位の管理構造

各プロジェクトは以下の論理領域を持つ。

* Input
* Working
* Output
* Chat
* Metadata

### 13.2 保存構成例

```text
/projects/{project\_id}/
  /input/
  /working/
    /index/
    /chunks/
    /ast/
    /callgraph/
    /ui-analysis/
    /db-analysis/
    /drafts/
  /output/
    /md/
    /puml/
    /svg/
    /json/
  /chat/
  /meta/
```

### 13.3 中間成果物

中間成果物は以下を想定する。

* ファイルメタデータ
* AST 風構造
* シンボルテーブル
* 呼び出しグラフ
* クラス関係
* 画面候補一覧
* テーブル操作一覧
* 推定根拠一覧
* 文書草稿

### 13.4 中間データの構造定義

エージェント間の中間データは JSON 形式で統一する。主要な中間データの構造を以下に定義する。

#### 13.4.1 クラス情報（class\_info.json）

```json
{
  "id": "cls_001",
  "name": "UserService",
  "fqn": "com.example.service.UserService",
  "filePath": "src/main/java/com/example/service/UserService.java",
  "language": "java",
  "type": "class | interface | enum | struct",
  "modifiers": ["public"],
  "superClass": { "name": "BaseService", "fqn": "com.example.base.BaseService", "resolved": true },
  "interfaces": [{ "name": "IUserService", "fqn": "com.example.api.IUserService", "resolved": true }],
  "fields": [
    { "name": "userRepository", "type": "UserRepository", "fqn": "com.example.repo.UserRepository", "resolved": true, "modifiers": ["private"] }
  ],
  "methods": ["mtd_001", "mtd_002"],
  "layer": "Service",
  "responsibility": "ユーザの登録・更新・削除を管理する [推定]",
  "confidence": 0.85,
  "evidences": ["ev_001"],
  "status": "success"
}
```

#### 13.4.2 メソッド情報（method\_info.json）

```json
{
  "id": "mtd_001",
  "name": "createUser",
  "classId": "cls_001",
  "filePath": "src/main/java/com/example/service/UserService.java",
  "lineStart": 25,
  "lineEnd": 45,
  "returnType": "User",
  "parameters": [
    { "name": "request", "type": "CreateUserRequest" }
  ],
  "modifiers": ["public"],
  "callsTo": [
    { "methodId": "mtd_010", "callType": "direct", "lineNumber": 32, "resolved": true, "confidence": 1.0 },
    { "methodId": "mtd_020", "callType": "interface", "lineNumber": 35, "resolved": false, "candidates": ["mtd_021", "mtd_022"], "confidence": 0.6 }
  ],
  "calledBy": ["mtd_100"],
  "dbOperations": ["INSERT:users"],
  "summary": "ユーザ情報をバリデーションし、DBに登録する [推定]",
  "confidence": 0.8,
  "evidences": ["ev_002"]
}
```

#### 13.4.3 呼び出し関係（call\_graph.json）

```json
{
  "callerId": "mtd_001",
  "calleeId": "mtd_010",
  "callType": "direct | interface | virtual | function_pointer | unknown",
  "lineNumber": 32,
  "isConditional": false,
  "resolved": true,
  "confidence": 1.0
}
```

#### 13.4.4 推定根拠（evidence.json）

```json
{
  "id": "ev_001",
  "projectId": "proj_001",
  "targetId": "cls_001",
  "evidenceType": "naming | annotation | import | code_pattern | llm_inference",
  "sourcePath": "src/main/java/com/example/service/UserService.java",
  "lineRange": [1, 10],
  "summary": "クラス名 'UserService' + @Service アノテーションから Service 層と判定",
  "confidence": 0.85
}
```

#### 13.4.5 解析ステータス（analysis\_status.json）

```json
{
  "fileStatuses": [
    { "filePath": "src/main/java/...", "status": "success", "symbolCount": 12 },
    { "filePath": "src/main/c/legacy.c", "status": "partial", "reason": "macro_heavy", "symbolCount": 5 },
    { "filePath": "src/main/c/broken.c", "status": "failed", "reason": "parse_error", "errorDetail": "..." }
  ],
  "totalFiles": 150,
  "successCount": 140,
  "partialCount": 8,
  "failedCount": 2
}
```

\---

## 14\. 解析対象の詳細仕様

### 14.1 C / C++ 解析

抽出対象:

* struct
* class
* enum
* 関数
* メソッド
* include 関係
* ヘッダ / 実装対応
* グローバル変数
* マクロ
* ファイル責務

推定対象:

* モジュール境界
* 処理入口
* 共有状態
* 画面処理らしき箇所
* DB アクセスらしき箇所

### 14.2 Java 解析

抽出対象:

* class
* interface
* enum
* method
* package
* annotation
* import
* 継承 / 実装
* Bean / Controller / Service / Repository 候補
* Entity 候補
* Mapper 候補

推定対象:

* MVC / layered architecture
* Spring 系利用有無
* MyBatis 利用有無
* JPA 利用有無
* 画面テンプレートとの対応

### 14.3 DB 関連解析

対象:

* SQL ファイル
* XML Mapper
* アノテーション SQL
* Repository / DAO / Mapper
* テーブル名
* カラム名
* INSERT / UPDATE / DELETE / SELECT

出力:

* CRUD 対応表
* DB 更新仕様
* 更新トリガ元メソッド
* 関連画面または API 推定

### 14.4 UI 関連解析

対象:

* HTML
* JSP
* Thymeleaf 等のテンプレート
* Swing / 独自 GUI の文字列や部品名
* イベントハンドラ
* 画面 ID や遷移先定義

出力:

* 画面遷移図
* 画面項目仕様
* イベント一覧
* レイアウト概略

\---

## 15\. 出力ドキュメント仕様

### 15.0 共通仕様

全出力ドキュメントに以下の共通ルールを適用する。

* 推定情報には `[推定]` マーカーを付与する
* 推定情報には confidence 値（0.0〜1.0）を付記する（例: `[推定: 0.85]`）
* コードから機械的に抽出した確定情報にはマーカーを付けない
* 推定の根拠（Evidence）への参照を脚注として付与する
* 解析できなかった箇所は `[未解析]` マーカーで明示する

## 15.1 全体処理概要

内容:

* システムの主要機能
* エントリーポイント
* 起動から終了までの主要処理
* バッチ / 画面 / API ごとの流れ

形式:

* Markdown
* 必要に応じて補助シーケンス図

## 15.2 クラス一覧

内容:

* クラス名
* ファイルパス
* 役割
* 継承 / 実装
* 関連クラス

## 15.3 クラス図

形式:

* PlantUML
* SVG 変換

内容:

* 継承
* 実装
* 集約 / 依存
* 主なメソッド

## 15.4 クラス仕様

内容:

* クラス概要
* 責務
* 主な属性
* 主なメソッド
* 呼び出し元 / 呼び出し先
* 使用テーブル / 画面との関係

## 15.5 メソッド一覧

内容:

* メソッド名
* 所属クラス
* 引数
* 戻り値
* 概要
* 呼び出し関係
* DB 更新有無
* 画面イベント関連有無

## 15.6 メソッド処理概要

内容:

* 前提条件
* 主処理
* 分岐
* 例外系
* 後続処理

## 15.7 フローチャート

形式:

* PlantUML アクティビティ図 or flowchart 記法
* SVG 変換

## 15.8 シーケンス図

対象:

* 代表ユースケースごと
* 主要イベントごと
* DB 更新系ごと

## 15.9 画面遷移図

内容:

* 画面 ID
* 遷移元
* 遷移先
* 遷移契機

## 15.10 画面項目仕様書

内容:

* 項目 ID
* 項目名
* 型
* 必須
* 初期値
* 入力制約
* 関連イベント
* 関連 DB 項目

## 15.11 画面イベント一覧

内容:

* イベント名
* 対象画面
* 発火条件
* 呼出メソッド
* 更新対象

## 15.12 画面レイアウト

内容:

* 画面の簡易構成
* 項目ブロック
* ボタン配置
* 一覧 / 明細構成

形式:

* Markdown ベースの簡易モック
* 可能なら PlantUML / mermaid で補助表現

## 15.13 CRUD表

内容:

* 機能 / 画面 / メソッド × テーブル
* Create / Read / Update / Delete の有無
* 根拠メソッド

## 15.14 DB更新仕様

内容:

* 更新対象テーブル
* 更新契機
* 実行メソッド
* SQL 概要
* 条件
* トランザクション境界推定

\---

## 16\. UI 要件

### 16.1 画面一覧

* ログインなしトップ相当画面またはプロジェクト一覧
* プロジェクト詳細画面
* アップロード画面
* 解析実行状況画面
* チャット画面
* 設計書閲覧画面
* 中間成果物閲覧画面
* ジョブ履歴画面

### 16.2 プロジェクト一覧画面

機能:

* プロジェクト一覧表示
* 新規作成
* 最近更新順表示
* ステータス表示

### 16.3 プロジェクト詳細画面

表示:

* 概要
* 技術スタック推定
* 最終解析日時
* 入力ファイル件数
* 出力ドキュメント件数

### 16.4 アップロード画面

機能:

* zip アップロード
* 複数ファイルアップロード
* 解析開始指定
* 上書き / 新バージョン選択

### 16.5 チャット画面

機能:

* ユーザ指示入力
* エージェントの要約ログ表示
* 途中経過表示
* 参照中成果物へのリンク表示

表示方針:

* 内部思考の全文は不要
* 要点ログのみ表示
* どのエージェントが何をしたかを簡潔表示

### 16.6 設計書閲覧画面

機能:

* Markdown プレビュー
* SVG 図表示
* 文書一覧表示
* 再生成ボタン
* ダウンロード

### 16.7 中間成果物閲覧画面

対象:

* 抽出クラス一覧
* 呼び出しグラフ断片
* 推定技術スタック
* 解析警告一覧

\---

## 17\. API 要件

### 17.1 プロジェクトAPI

* `POST /projects`
* `GET /projects`
* `GET /projects/{id}`
* `PATCH /projects/{id}`

### 17.2 アップロードAPI

* `POST /projects/{id}/upload`
* `POST /projects/{id}/versions`

### 17.3 解析API

* `POST /projects/{id}/analyze`
* `GET /projects/{id}/jobs`
* `GET /jobs/{id}`

### 17.4 チャットAPI

* `POST /projects/{id}/chat`
* `GET /projects/{id}/chat/messages`

### 17.5 成果物API

* `GET /projects/{id}/artifacts`
* `GET /projects/{id}/artifacts/{artifact\_id}`
* `POST /projects/{id}/artifacts/regenerate`

\---

## 18\. 内部データモデル

本システムでは RDB を使用せず、すべてのデータを JSON ファイルとしてプロジェクトディレクトリ配下に保存する。  
各エンティティは `/meta/` ディレクトリ内の JSON ファイルとして管理する。

### 18.1 Project（`/meta/project.json`）

* id
* name
* description
* status
* created\_at
* updated\_at
* active\_version\_id

### 18.2 ProjectVersion（`/meta/versions.json`）

* id
* project\_id
* label
* upload\_path
* created\_at

### 18.3 AnalysisJob（`/meta/jobs.json`）

* id
* project\_id
* version\_id
* status
* started\_at
* finished\_at
* current\_phase
* summary

### 18.4 Artifact（`/meta/artifacts.json`）

* id
* project\_id
* version\_id
* type
* path
* format
* created\_at

### 18.5 ChatMessage（`/chat/messages.json`）

* id
* project\_id
* role
* message
* related\_job\_id
* created\_at

### 18.6 Evidence（`/meta/evidences.json`）

* id
* project\_id
* source\_path
* symbol\_name
* evidence\_type
* summary
* confidence

### 18.7 プロジェクト一覧管理（`/projects/index.json`）

全プロジェクトの一覧を管理するインデックスファイル。

* projects\[\]
  * id
  * name
  * status
  * updated\_at

\---

## 19\. 処理フロー仕様

### 19.1 初回解析フロー

1. プロジェクト作成
2. ファイルアップロード
3. 展開・前処理
4. 言語 / 技術判定
5. シンボル抽出
6. 呼び出し関係抽出
7. UI / DB 解析
8. 文書構造生成
9. Markdown / PlantUML 生成
10. SVG 変換
11. レビュー
12. 出力保存
13. UI 反映

### 19.2 チャット再解析フロー

1. ユーザ指示受領
2. 対象成果物特定
3. 関連ソース再読込
4. 必要なエージェントのみ再実行
5. 成果物差し替え
6. 回答返却

\---

## 20\. 推奨技術スタック

### 20.1 フロントエンド

* React
* Markdown Viewer
* SVG Viewer
* チャット UI コンポーネント

### 20.2 バックエンド

* typescript　
* Python
* Vercel AI SDK
* 設計書作成時のプロンプトテンプレート管理
* ジョブキュー
* ファイル管理ストレージ

### 20.3 解析基盤

* Tree-sitter などの構文解析器（Layer 1: 構文解析）
* 自前シンボル解決エンジン（Layer 2: import/include 追跡、型解決）
* ctags / clang 系補助（Layer 2 の精度向上）
* Java Parser 系（Layer 2 の精度向上）
* PlantUML
* SVG 変換環境

### 20.4 LLM 連携

* Azure OpenAI Adapter
* Ollama Adapter

\---

## 21\. 実装優先順位

### 21.1 Phase 1

* プロジェクト管理
* アップロード
* C / C++ / Java の基本解析
* クラス一覧
* メソッド一覧
* 全体処理概要
* クラス図
* CRUD表
* DB更新仕様の基礎

### 21.2 Phase 2

* 画面遷移図
* 画面項目仕様
* イベント一覧
* フローチャート
* シーケンス図
* チャット再解析

### 21.3 Phase 3

* 差分再解析
* 文書品質レビュー
* 推定信頼度表示
* 複数モデル切替最適化
* 文書テンプレート切替

\---

## 22\. 受入条件

以下を満たした場合、本仕様の MVP 実装は受入可能とする。

1. プロジェクト単位でアップロードできる
2. C / C++ / Java の少なくとも基本構造を抽出できる
3. クラス一覧、メソッド一覧、クラス図、全体処理概要を生成できる
4. CRUD表、DB更新仕様の初版を生成できる
5. Markdown と SVG を Web UI で閲覧できる
6. チャットから追加指示を出せる
7. プロジェクトごとに Input / Working / Output が管理される

\---

## 23\. 制約事項

* 完全な意味理解は保証しない
* レガシーコードの品質によって推定精度は変動する
* マクロ多用、動的ロード、多段生成コードでは解析精度が下がる
* 画面遷移や DB 更新は推定になる場合がある
* 信頼度と根拠の保持が必須
* MVP では RDB を使用しない。データはすべて JSON ファイルとしてファイルシステム上で管理する
* MVP では RAG（Retrieval-Augmented Generation）を使用しない。既存ドキュメント統合は今後の拡張案（§24）とする
* 静的解析の限界として、以下は解析精度が低下する可能性がある
  * ポリモーフィズム（インターフェース経由の呼出しは実装候補の列挙にとどまる）
  * リフレクション / 動的プロキシ
  * C/C++ のマクロ多用コード（プリプロセス前のコードを解析するため）
  * DI コンテナによる依存注入（設定ファイルベースの場合）
  * 関数ポインタ / コールバック経由の呼出し
* これらの限界箇所は呼び出しグラフ上で `confidence` 値を低く設定し、出力ドキュメントで `[推定]` マーカーを付与して明示する

\---

## 24\. 今後の拡張案

* C#、VB、Python などの言語拡張
* API 一覧 / バッチ一覧の自動生成
* テストケース候補生成
* リファクタリング提案
* 変更影響分析
* RAG による既存ドキュメント統合
* チーム共有コメント機能
* エクスポート形式拡張
* **意味解析の高度化**
  * C/C++ プリプロセス対応（`gcc -E` / `clang -E` 相当の展開後コードの解析）
  * ポリモーフィズムの高精度解決（DI 設定ファイル解析、実行時型推論）
  * Language Server Protocol（LSP）連携による型解決精度の向上
  * clang の libTooling / Java の Eclipse JDT による本格的な意味解析エンジン
  * 動的解析（テスト実行時のトレース情報）との統合
* **ユーザフィードバックの構造化**
  * 推定結果に対するユーザの承認・修正・補足の保持
  * フィードバックを次回解析に反映するルール管理
* **解析キャッシュの高度化**
  * ファイルハッシュベースの Tree-sitter 解析結果キャッシュ
  * LLM 推論結果のキャッシュ（同一入力に対する再利用）

\---

## 25\. 実装上の補足指針

* まずは中間構造の設計（§13.4 のスキーマ定義）を優先する
* 文書生成は中間構造からの一方向生成にする
* エージェント同士で直接ファイル編集させず、オーケストレータ経由にする
* 解析結果は常に evidence を紐付ける
* ユーザ補足情報はプロジェクトメタ情報として保持する
* UI では「確定情報」と「推定情報」を分けて表示する
* LLM で生成した PlantUML は必ず構文検証を行い、エラーがあれば LLM にエラー内容を渡して再生成させる（最大 3 回リトライ）
* データ永続化は JSON ファイルベースとし、ORM / マイグレーションは使用しない
* **LLM プロンプト設計指針**:
  * エージェントごとにプロンプトテンプレートを用意し、テンプレートファイルとして管理する
  * 出力は JSON Schema で構造化出力を強制する（自由文ではなく型付きデータで受け取る）
  * 解析対象コードだけでなく、関連クラスのシグネチャ・呼出元情報をコンテキストに含める
  * Few-shot 例をプロンプトに含めて出力品質を安定させる
  * 推定結果には必ず `confidence` フィールドを出力させる
* **呼び出しグラフの精度管理**:
  * 静的解析で解決できた呼出し: `resolved: true, confidence: 1.0`
  * インターフェース経由で候補が特定できた呼出し: `resolved: false, candidates: [...], confidence: 0.5-0.8`
  * 解決不能な呼出し（関数ポインタ等）: `resolved: false, confidence: 0.0-0.3`
* **解析の部分成功を許容する**: 一部ファイルの解析失敗は全体を停止させず、成功分で出力を生成する（§9.5）

\---

## 26\. まとめ

本システムは、レガシーな C / C++ / Java プロジェクトを対象に、失われた設計書を復元するための **ハイブリッド型マルチエージェント Web アプリ** である。  
Web UI によるプロジェクト単位の管理、チャットによる追加入力、Input / 中間成果物 / Output の分離管理、Markdown / PlantUML / SVG による設計書生成を中核とする。

実装の中心は以下である。

* ワークフロー型で全体制御
* ReAct 型で探索解析
* マルチエージェントで責務分離
* モデル切替可能な抽象化
* 中間成果物中心の設計
* プロジェクトごとの継続的な再解析

以上を本仕様の初版とする。

